{
    "name": "tamagitto-extension",
    "version": "1.0.0",
    "description": "Tamagitto Chrome Extension",
    "scripts": {
        "lint": "eslint *.js",
        "lint:fix": "eslint *.js --fix",
        "test": "jest",
        "test:watch": "jest --watch"
    },
    "devDependencies": {
        "eslint": "^8.50.0",
        "jest": "^29.7.0",
        "prettier": "^3.0.3"
    },
    "jest": {
        "testEnvironment": "jsdom",
        "setupFilesAfterEnv": ["<rootDir>/tests/setup.js"]
    }
}